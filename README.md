# Chopinow Frontend

## Development
1. Copy `.env.example` to `.env.local`
2. Run:
   npm install
   npm run dev
3. Open http://localhost:3000

## Deployment
- Connect repo to Vercel
- Add env variables
- Add staging.chopinow.com as custom domain
